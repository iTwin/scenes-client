---
"@itwin/scenes-client": minor
---

 **BREAKING CHANGE**: Remove deprecated `visible` property from `Layer`, `Repository`, and `RepositoryResource` SceneObject data types.
 - Consumers must use `visible` field in top-level object metadata instead.
